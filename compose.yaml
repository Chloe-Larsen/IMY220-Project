version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: tiptap-backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    networks:
      - tiptap-network
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: tiptap-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
    networks:
      - tiptap-network
    restart: unless-stopped

networks:
  tiptap-network:
    driver: bridge